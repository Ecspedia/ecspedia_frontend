export type Flight = {
  id: number;
  departureLocation: string;
  arrivalLocation: string;
  departureTime: string;
  arrivalTime: string;
  company: string;
  price: number;
  totalTime: string;
  image: string;
};
