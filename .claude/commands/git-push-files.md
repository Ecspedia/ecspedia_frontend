## Command: git-push-files

Push specific files to remote.

1. Run `git status` to show current changes
2. Ask the user which files to stage
3. Run `git add` for each specified file
4. Ask the user for a commit message
5. Create the commit with the provided message
6. Run `git push` to push to the current branch
